package com.jesm3.newDualis.synchronization;

import java.util.Timer;

public class AutoSync {
	private Timer timer = new Timer();

	public void startTimer(int timespan) {
		// timer.schedule(task, when, period)
	}
}
